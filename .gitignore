node_modules/
dist/
.astro/
# Astro build cache (astro.config.mjs → cacheDir) + npm download cache
# (amplify.yml → preBuild) — both project-root so they survive `npm ci`.
.astro-cache/
.npm/
.env
.env.*
!.env.example
# Cloudflare local secrets (wrangler / astro dev platformProxy) + build output.
.dev.vars
.dev.vars.*
!.dev.vars.example
.wrangler/
# verify-donate screenshot output
scripts/screenshots/
